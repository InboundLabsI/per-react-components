{
  "name": "permobil-navigator-components",
  "version": "1.2.3",
  "private": true,
  "source": "src/index.js",
  "dependencies": {
    "@inboundlabs/per-react-components": "file:../",
    "react": "file:../node_modules/react",
    "react-dom": "file:../node_modules/react-dom",
    "react-scripts": "file:../node_modules/react-scripts"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "build:widget": "microbundle --entry src/index.js --jsx React.createElement --css-modules false --external none --target web --f umd"
  },
  "files": [
    "dist"
  ],
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "microbundle": "^0.13.0",
    "sass": "^1.30.0"
  }
}